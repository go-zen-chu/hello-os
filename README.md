# hello-os

Just an OS running on QEMU

## setup

```
brew install nasm
brew install qemu
```

## boot os

```
make build
make run
```

When the OS starts, a simple terminal prompt appears. Try `help` and `clear`.
